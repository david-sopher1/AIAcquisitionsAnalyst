# Internal URL of the DealEngine backend API (server-side only, never sent to the browser)
API_INTERNAL_URL=http://localhost:4000

# API key forwarded as x-api-key on every proxied request (server-side only)
DASHBOARD_API_KEY=
